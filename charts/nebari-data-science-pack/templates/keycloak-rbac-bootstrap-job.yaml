{{/*
Keycloak realm bootstrap — runs once per chart install/upgrade.

The actual logic lives in ``files/keycloak_rbac_bootstrap.py`` (a
plain Python module using only the standard library, unit-tested in
``tests/unit/test_keycloak_rbac_bootstrap.py``). The Helm chart's
sole responsibility is to:

  1. ship that file into the cluster as a ConfigMap, and
  2. run it inside a vanilla ``python:3.x-slim`` container with the
     right env vars wired from chart values + the deployer-supplied
     admin-password Secret.

This split keeps the realm provisioning code out of YAML bash heredocs
where it cannot be tested, linted, or single-stepped. Bumping the
Python image tag forces a checksum-driven Job re-roll on the next
chart upgrade.

Gated on:
  - ``rbac.bootstrap.enabled`` AND
  - the deployer providing a KC admin password Secret reference AND
  - ``nebariapp.enabled`` — no Nebari operator / KC realm means there
    is nothing to bootstrap; lets kind / local-dev installs come up
    cleanly without the post-install Job hitting a missing namespace.
The chart deploys fine without RBAC bootstrap pre-configured.
*/}}
{{- if and .Values.nebariapp.enabled .Values.rbac.bootstrap.enabled .Values.rbac.bootstrap.kcAdminCredentialSecret }}
{{/*
The hub OIDC client id is read at runtime from the operator-provisioned
OIDC client Secret (the same Secret the hub mounts at /etc/oauth), so
the Job never has to guess the operator's client-naming convention.
``rbac.bootstrap.hubClientId`` remains as an explicit override that
skips the Secret read entirely.
*/}}
{{- $jobNamespace := .Values.rbac.bootstrap.namespace | default .Release.Namespace }}
{{- $oidcSecretName := .Values.rbac.bootstrap.oidcClientSecretName | default (printf "%s-oidc-client" (include "nebari-data-science-pack.fullname" .)) }}
{{- $script := .Files.Get "files/keycloak_rbac_bootstrap.py" }}
{{- if not $script }}
{{- fail "files/keycloak_rbac_bootstrap.py missing from the chart — repackage with the file included." }}
{{- end }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "nebari-data-science-pack.fullname" . }}-rbac-bootstrap
  {{- with .Values.rbac.bootstrap.namespace }}
  namespace: {{ . | quote }}
  {{- end }}
  labels:
    {{- include "nebari-data-science-pack.labels" . | nindent 4 }}
    app.kubernetes.io/component: rbac-bootstrap
  annotations:
    "helm.sh/hook": post-install,post-upgrade
    "helm.sh/hook-weight": "0"
    "helm.sh/hook-delete-policy": before-hook-creation
data:
  keycloak_rbac_bootstrap.py: |
{{ $script | indent 4 }}
---
{{/*
The Job usually runs in the ``keycloak`` namespace (to read the KC
admin Secret via env) while the operator writes the OIDC client Secret
into the release namespace, a cross-namespace read that env
``secretKeyRef`` can't do. The script reads it through the K8s API
instead, so the Job gets a ServiceAccount plus a get-one-secret Role
in the release namespace.
*/}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "nebari-data-science-pack.fullname" . }}-rbac-bootstrap
  namespace: {{ $jobNamespace | quote }}
  labels:
    {{- include "nebari-data-science-pack.labels" . | nindent 4 }}
    app.kubernetes.io/component: rbac-bootstrap
  annotations:
    "helm.sh/hook": post-install,post-upgrade
    "helm.sh/hook-weight": "0"
    "helm.sh/hook-delete-policy": before-hook-creation
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ include "nebari-data-science-pack.fullname" . }}-rbac-bootstrap-oidc-read
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "nebari-data-science-pack.labels" . | nindent 4 }}
    app.kubernetes.io/component: rbac-bootstrap
  annotations:
    "helm.sh/hook": post-install,post-upgrade
    "helm.sh/hook-weight": "0"
    "helm.sh/hook-delete-policy": before-hook-creation
rules:
  - apiGroups: [""]
    resources: ["secrets"]
    resourceNames: [{{ $oidcSecretName | quote }}]
    verbs: ["get"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "nebari-data-science-pack.fullname" . }}-rbac-bootstrap-oidc-read
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "nebari-data-science-pack.labels" . | nindent 4 }}
    app.kubernetes.io/component: rbac-bootstrap
  annotations:
    "helm.sh/hook": post-install,post-upgrade
    "helm.sh/hook-weight": "0"
    "helm.sh/hook-delete-policy": before-hook-creation
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ include "nebari-data-science-pack.fullname" . }}-rbac-bootstrap-oidc-read
subjects:
  - kind: ServiceAccount
    name: {{ include "nebari-data-science-pack.fullname" . }}-rbac-bootstrap
    namespace: {{ $jobNamespace | quote }}
---
apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "nebari-data-science-pack.fullname" . }}-rbac-bootstrap
  {{- with .Values.rbac.bootstrap.namespace }}
  namespace: {{ . | quote }}
  {{- end }}
  labels:
    {{- include "nebari-data-science-pack.labels" . | nindent 4 }}
    app.kubernetes.io/component: rbac-bootstrap
  annotations:
    "helm.sh/hook": post-install,post-upgrade
    "helm.sh/hook-weight": "5"
    # Keep the Job around after success so its logs stay inspectable.
    # Delete only before the next hook fires (next install/upgrade).
    "helm.sh/hook-delete-policy": before-hook-creation
    # Force a re-run when the script content changes — Helm hook re-uses
    # the same Job name across upgrades and would otherwise skip if the
    # spec hash matches. Hash the script + values so a chart-only change
    # also re-runs.
    checksum/script: {{ $script | sha256sum }}
spec:
  ttlSecondsAfterFinished: 600
  backoffLimit: 2
  template:
    metadata:
      labels:
        {{- include "nebari-data-science-pack.labels" . | nindent 8 }}
        app.kubernetes.io/component: rbac-bootstrap
    spec:
      restartPolicy: Never
      serviceAccountName: {{ include "nebari-data-science-pack.fullname" . }}-rbac-bootstrap
      containers:
        - name: bootstrap
          image: {{ .Values.rbac.bootstrap.image | quote }}
          command:
            - python
            - /scripts/keycloak_rbac_bootstrap.py
          env:
            - name: KC_HOST
              value: {{ .Values.rbac.bootstrap.kcHost | quote }}
            - name: KC_ADMIN_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.rbac.bootstrap.kcAdminCredentialSecret | quote }}
                  key: {{ .Values.rbac.bootstrap.kcAdminCredentialSecretKey | default "password" | quote }}
            - name: REALM
              value: {{ .Values.rbac.bootstrap.realmName | quote }}
            {{- with .Values.rbac.bootstrap.hubClientId }}
            - name: HUB_CLIENT_ID
              value: {{ . | quote }}
            {{- end }}
            - name: HUB_CLIENT_SECRET_NAME
              value: {{ $oidcSecretName | quote }}
            - name: HUB_CLIENT_SECRET_NAMESPACE
              value: {{ .Release.Namespace | quote }}
            - name: ROLE_NAME
              value: {{ .Values.rbac.bootstrap.sharedMountRoleName | quote }}
            - name: SHARED_MOUNT_GROUPS
              value: {{ .Values.rbac.bootstrap.sharedMountGroups | join "," | quote }}
            - name: HUB_EXTERNAL_URL
              {{- /* Defaults to the derived external hub URL (nebariapp.hostname,
              falling back to hub.<base-domain-of-keycloak.hostname>) so the
              bootstrap can patch rootUrl / baseUrl / initiate.login.uri on the
              hub OIDC client. Empty value disables the patch. Must go through
              the hubExternalURL helper, NOT nebariapp.hostname directly: with
              the hostname left to derivation the raw value renders "https://",
              which the script's rstrip("/") reduces to "https:" - truthy, so
              the empty-value guard never fires and Keycloak 400s the reconcile
              (issue #185). */}}
              value: {{ .Values.rbac.bootstrap.hubExternalUrl | default (include "nebari-data-science-pack.hubExternalURL" . | trimSuffix "/") | quote }}
            - name: PYTHONUNBUFFERED
              value: "1"
          volumeMounts:
            - name: script
              mountPath: /scripts
              readOnly: true
      volumes:
        - name: script
          configMap:
            name: {{ include "nebari-data-science-pack.fullname" . }}-rbac-bootstrap
            defaultMode: 0o0555
{{- end }}
